# Secure Bootstrap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a secure, GitHub Pages-ready Bet Buddy project foundation with React, TypeScript, Vite, documented security controls, and CI gates.

**Architecture:** Bootstrap the repository first, then add the minimal frontend toolchain, then wire security controls required by `SECURITY-SSOT.md`. The first deliverable is not the full game; it is a reproducible, testable, deployable foundation that later MVP game tasks can build on safely.

**Tech Stack:** Git, GitHub Pages, GitHub Actions, Node.js 20, npm, Vite, React, TypeScript, Tailwind CSS, Vitest, Zustand, Howler.js, Lucide React, vite-plugin-pwa.

---

## File Structure

- Existing: `docs/BET-BUDDY-KONZEPTPAPIER.md` is the product concept and remains in the `docs/` directory.
- Existing: `docs/SECURITY-SSOT.md` is the binding security source of truth and remains in the `docs/` directory.
- Create: `.gitignore` excludes Node, Vite, OS, editor, test, and build artifacts.
- Create: `.npmrc` sets `ignore-scripts=true` as required by `SECURITY-SSOT.md`.
- Create: `.nojekyll` disables GitHub Pages Jekyll processing.
- Create: `package.json` defines scripts for development, typecheck, test, security check, build, and preview.
- Create: `package-lock.json` is generated by npm and committed.
- Create: `index.html` includes CSP and referrer policy meta tags before scripts.
- Create: `vite.config.ts` configures React, PWA, GitHub Pages base path, no production source maps, and console/debugger removal.
- Create: `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json` define strict TypeScript settings.
- Create: `src/main.tsx`, `src/App.tsx`, `src/index.css`, `src/vite-env.d.ts` provide a minimal app shell.
- Create: `src/security/validatePlayerName.ts` contains the first security-critical input validation helper.
- Create: `src/security/validatePlayerName.test.ts` tests the player-name validation rules.
- Create: `scripts/security-check.sh` blocks forbidden patterns in `src/`.
- Create: `scripts/validate-csp.mjs` validates CSP in `dist/index.html` after build.
- Create: `.github/workflows/deploy.yml` runs CI and deploys to GitHub Pages.
- Create: `.github/dependabot.yml` enables dependency and GitHub Actions update monitoring.
- Create: `README.md` documents local development, security posture, and deployment.

---

### Task 1: Initialize Local Git Repository

**Files:**
- No source files changed by this task.
- Git metadata created in `.git/`.

- [ ] **Step 1: Verify the workspace is not already a Git repository**

Run:

```powershell
git status --short
```

Expected:

```text
fatal: not a git repository (or any of the parent directories): .git
```

- [ ] **Step 2: Initialize Git**

Run:

```powershell
git init
```

Expected:

```text
Initialized empty Git repository in C:/Users/pierr/Desktop/BetBuddies/.git/
```

- [ ] **Step 3: Set the default branch to `main`**

Run:

```powershell
git branch -M main
```

Expected: command exits with code `0`.

- [ ] **Step 4: Verify no GitHub remote is configured yet**

Run:

```powershell
git remote -v
```

Expected: no output.

- [ ] **Step 5: Commit repository planning documents locally**

Run:

```powershell
git add docs/BET-BUDDY-KONZEPTPAPIER.md docs/SECURITY-SSOT.md docs/2026-05-04-secure-bootstrap.md
git commit -m "docs: add concept and security sources of truth"
```

Expected: commit succeeds.

---

### Task 2: Add Repository Guardrail Files

**Files:**
- Create: `.gitignore`
- Create: `.npmrc`
- Create: `.nojekyll`

- [ ] **Step 1: Create `.gitignore`**

Create `.gitignore` with:

```gitignore
# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
coverage/
playwright-report/
test-results/

# Environment files
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo
```

- [ ] **Step 2: Create `.npmrc`**

Create `.npmrc` with:

```ini
ignore-scripts=true
fund=false
audit=true
```

- [ ] **Step 3: Create `.nojekyll`**

Create an empty `.nojekyll` file.

- [ ] **Step 4: Verify files exist**

Run:

```powershell
Get-ChildItem -Force .gitignore,.npmrc,.nojekyll
```

Expected: all three files are listed.

- [ ] **Step 5: Commit guardrail files**

Run:

```powershell
git add .gitignore .npmrc .nojekyll
git commit -m "chore: add repository guardrails"
```

Expected: commit succeeds.

---

### Task 3: Scaffold Vite React TypeScript App

**Files:**
- Create: `package.json`
- Create: `index.html`
- Create: `src/main.tsx`
- Create: `src/App.tsx`
- Create: `src/index.css`
- Create: `src/vite-env.d.ts`

- [ ] **Step 1: Create `package.json`**

Create `package.json` with:

```json
{
  "name": "bet-buddy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "test:watch": "vitest",
    "security:check": "bash scripts/security-check.sh",
    "security:csp": "node scripts/validate-csp.mjs",
    "build": "npm run typecheck && vite build && npm run security:csp",
    "preview": "vite preview"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite-plugin-pwa": "^1.0.3",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@types/howler": "^2.2.12",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.2",
    "tailwindcss": "^4.2.2",
    "typescript": "^5.7.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
```

- [ ] **Step 2: Confirm `package.json` contains no `latest` tags**

Run:

```powershell
Select-String -Path "package.json" -Pattern "latest"
```

Expected: no output.

- [ ] **Step 3: Install dependencies**

Run:

```powershell
npm install
```

Expected:

```text
added ... packages
found 0 vulnerabilities
```

If the sandbox blocks network access, rerun with escalated approval.

- [ ] **Step 4: Create `index.html`**

Create `index.html` with:

```html
<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self'; media-src 'self'; font-src 'self'; manifest-src 'self'; worker-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'self'; form-action 'none';"
    />
    <meta name="referrer" content="no-referrer" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#1A1A2E" />
    <title>Bet Buddy</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: Create `src/main.tsx`**

Create `src/main.tsx` with:

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 6: Create `src/App.tsx`**

Create `src/App.tsx` with:

```tsx
export default function App() {
  return (
    <main className="app-shell">
      <section className="start-screen" aria-labelledby="app-title">
        <p className="eyebrow">Partyspiel</p>
        <h1 id="app-title">Bet Buddy</h1>
        <p className="intro">
          Setzt Teams, bietet mutig und zeigt, wie gut ihr eure Buddies einschätzen könnt.
        </p>
        <button type="button" className="primary-action">
          Neues Spiel
        </button>
      </section>
    </main>
  );
}
```

- [ ] **Step 7: Create `src/index.css`**

Create `src/index.css` with:

```css
:root {
  color-scheme: dark;
  font-family:
    Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
    sans-serif;
  background: #1a1a2e;
  color: #eaeaea;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  --team-a: #ff6b35;
  --team-b: #2ec4b6;
  --accent: #e94560;
  --surface: #16213e;
}

* {
  box-sizing: border-box;
}

html,
body,
#root {
  min-height: 100%;
  margin: 0;
}

button,
input,
select {
  font: inherit;
}

.app-shell {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 24px;
  background:
    radial-gradient(circle at top left, rgb(255 107 53 / 18%), transparent 30%),
    radial-gradient(circle at bottom right, rgb(46 196 182 / 18%), transparent 32%),
    #1a1a2e;
}

.start-screen {
  width: min(100%, 520px);
  display: grid;
  gap: 20px;
  text-align: center;
}

.eyebrow {
  margin: 0;
  color: #2ec4b6;
  font-weight: 800;
  text-transform: uppercase;
}

h1 {
  margin: 0;
  font-size: 64px;
  line-height: 1;
}

.intro {
  margin: 0;
  color: #c7c7d4;
  font-size: 18px;
  line-height: 1.5;
}

.primary-action {
  min-height: 64px;
  border: 0;
  border-radius: 8px;
  background: var(--accent);
  color: white;
  font-weight: 800;
  cursor: pointer;
}
```

- [ ] **Step 8: Create `src/vite-env.d.ts`**

Create `src/vite-env.d.ts` with:

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 9: Run initial checks**

Run:

```powershell
npm run typecheck
npm test
```

Expected: typecheck succeeds and Vitest reports no test files or passes, depending on installed Vitest behavior.

- [ ] **Step 10: Commit scaffold**

Run:

```powershell
git add package.json package-lock.json index.html src
git commit -m "feat: scaffold React app shell"
```

Expected: commit succeeds.

---

### Task 4: Add TypeScript And Vite Configuration

**Files:**
- Create: `tsconfig.json`
- Create: `tsconfig.app.json`
- Create: `tsconfig.node.json`
- Create: `vite.config.ts`

- [ ] **Step 1: Create `tsconfig.json`**

Create `tsconfig.json` with:

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

- [ ] **Step 2: Create `tsconfig.app.json`**

Create `tsconfig.app.json` with:

```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `tsconfig.node.json`**

Create `tsconfig.node.json` with:

```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "noEmit": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 4: Create `vite.config.ts`**

Create `vite.config.ts` with:

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig(({ mode }) => ({
  base: '/bet-buddy/',
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg', 'sounds/*.mp3'],
      manifest: {
        name: 'Bet Buddy',
        short_name: 'Bet Buddy',
        description: 'Ein lokales Partyspiel für Teams und mutige Wetten.',
        theme_color: '#1A1A2E',
        background_color: '#1A1A2E',
        display: 'standalone',
        start_url: '/bet-buddy/',
        scope: '/bet-buddy/',
        icons: [],
      },
      workbox: {
        navigateFallback: '/bet-buddy/index.html',
        globPatterns: ['**/*.{js,css,html,svg,png,jpg,jpeg,webp,mp3,json}'],
      },
    }),
  ],
  build: {
    sourcemap: false,
  },
  esbuild: {
    drop: mode === 'production' ? ['console', 'debugger'] : [],
  },
  test: {
    environment: 'jsdom',
    globals: true,
  },
}));
```

- [ ] **Step 5: Run typecheck**

Run:

```powershell
npm run typecheck
```

Expected: `tsc --noEmit` exits with code `0`.

- [ ] **Step 6: Commit configuration**

Run:

```powershell
git add tsconfig.json tsconfig.app.json tsconfig.node.json vite.config.ts
git commit -m "chore: configure TypeScript and Vite"
```

Expected: commit succeeds.

---

### Task 5: Add First Security-Critical Validation Helper

**Files:**
- Create: `src/security/validatePlayerName.ts`
- Create: `src/security/validatePlayerName.test.ts`

- [ ] **Step 1: Write failing tests**

Create `src/security/validatePlayerName.test.ts` with:

```ts
import { describe, expect, it } from 'vitest';
import { validatePlayerName } from './validatePlayerName';

describe('validatePlayerName', () => {
  it('accepts a trimmed valid name', () => {
    expect(validatePlayerName('  Pierre  ')).toEqual({
      ok: true,
      value: 'Pierre',
    });
  });

  it('rejects empty names', () => {
    expect(validatePlayerName('   ')).toEqual({
      ok: false,
      error: 'Bitte gib einen Namen ein.',
    });
  });

  it('rejects names longer than 20 characters', () => {
    expect(validatePlayerName('ABCDEFGHIJKLMNOPQRSTU')).toEqual({
      ok: false,
      error: 'Namen dürfen maximal 20 Zeichen lang sein.',
    });
  });

  it('rejects HTML-like characters', () => {
    expect(validatePlayerName('<script>')).toEqual({
      ok: false,
      error: 'Namen dürfen nur Buchstaben, Zahlen, Leerzeichen, Bindestriche und Unterstriche enthalten.',
    });
  });

  it('accepts German umlauts and digits', () => {
    expect(validatePlayerName('Jörg 2')).toEqual({
      ok: true,
      value: 'Jörg 2',
    });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
npm test -- src/security/validatePlayerName.test.ts
```

Expected: fail because `validatePlayerName.ts` does not exist.

- [ ] **Step 3: Implement validation helper**

Create `src/security/validatePlayerName.ts` with:

```ts
export type ValidationResult =
  | { ok: true; value: string }
  | { ok: false; error: string };

const PLAYER_NAME_PATTERN = /^[\p{L}\p{N} _-]+$/u;
const MAX_PLAYER_NAME_LENGTH = 20;

export function validatePlayerName(rawName: string): ValidationResult {
  const name = rawName.trim();

  if (name.length === 0) {
    return { ok: false, error: 'Bitte gib einen Namen ein.' };
  }

  if (name.length > MAX_PLAYER_NAME_LENGTH) {
    return { ok: false, error: 'Namen dürfen maximal 20 Zeichen lang sein.' };
  }

  if (!PLAYER_NAME_PATTERN.test(name)) {
    return {
      ok: false,
      error:
        'Namen dürfen nur Buchstaben, Zahlen, Leerzeichen, Bindestriche und Unterstriche enthalten.',
    };
  }

  return { ok: true, value: name };
}
```

- [ ] **Step 4: Run tests to verify pass**

Run:

```powershell
npm test -- src/security/validatePlayerName.test.ts
```

Expected: all five tests pass.

- [ ] **Step 5: Commit validation helper**

Run:

```powershell
git add src/security/validatePlayerName.ts src/security/validatePlayerName.test.ts
git commit -m "feat: add player name validation"
```

Expected: commit succeeds.

---

### Task 6: Add Security Automation Scripts

**Files:**
- Create: `scripts/security-check.sh`
- Create: `scripts/validate-csp.mjs`

- [ ] **Step 1: Create `scripts/security-check.sh`**

Create `scripts/security-check.sh` with:

```bash
#!/usr/bin/env bash
set -euo pipefail

FORBIDDEN_PATTERNS=(
  'dangerouslySetInnerHTML'
  'innerHTML'
  'outerHTML'
  'insertAdjacentHTML'
  'document\.write'
  'eval\('
  'new Function'
  'setTimeout\("'
  'setInterval\("'
  'http://'
  'window\.open'
)

EXIT_CODE=0
for pattern in "${FORBIDDEN_PATTERNS[@]}"; do
  if grep -rnE --include='*.ts' --include='*.tsx' "$pattern" src/; then
    echo "SECURITY: Forbidden pattern found: $pattern"
    EXIT_CODE=1
  fi
done

exit "$EXIT_CODE"
```

- [ ] **Step 2: Create `scripts/validate-csp.mjs`**

Create `scripts/validate-csp.mjs` with:

```js
import { readFileSync } from 'node:fs';

const html = readFileSync('dist/index.html', 'utf8');

const requiredDirectives = [
  "default-src 'none'",
  "script-src 'self'",
  "connect-src 'self'",
  "object-src 'none'",
  "form-action 'none'",
];

const missing = requiredDirectives.filter((directive) => !html.includes(directive));

if (!html.includes('http-equiv="Content-Security-Policy"')) {
  missing.push('Content-Security-Policy meta tag');
}

if (missing.length > 0) {
  console.error(`CSP validation failed. Missing: ${missing.join(', ')}`);
  process.exit(1);
}

console.log('CSP validation passed.');
```

- [ ] **Step 3: Run security pattern check**

Run:

```powershell
npm run security:check
```

Expected: command exits with code `0` and no forbidden patterns are printed.

- [ ] **Step 4: Run build and CSP validation**

Run:

```powershell
npm run build
```

Expected:

```text
CSP validation passed.
```

- [ ] **Step 5: Commit security scripts**

Run:

```powershell
git add scripts/security-check.sh scripts/validate-csp.mjs
git commit -m "chore: add security automation checks"
```

Expected: commit succeeds.

---

### Task 7: Add GitHub Actions And Dependabot

**Files:**
- Create: `.github/workflows/deploy.yml`
- Create: `.github/dependabot.yml`

- [ ] **Step 1: Resolve current GitHub Action SHAs**

Resolve and record current commit SHAs for:

```text
actions/checkout@v4
actions/setup-node@v4
actions/upload-pages-artifact@v3
actions/deploy-pages@v4
```

Use GitHub release pages or `git ls-remote`.

Expected: each action is pinned to a full 40-character SHA in the workflow.

- [ ] **Step 2: Create `.github/workflows/deploy.yml`**

Create `.github/workflows/deploy.yml` with the resolved SHA pins:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions: {}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - run: npm audit --audit-level=high
      - run: npm run security:check
      - run: npm run build
      - uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa
        with:
          path: dist
      - uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e
```

- [ ] **Step 3: Verify pinned GitHub Action SHAs still resolve**

Run:

```powershell
git ls-remote https://github.com/actions/checkout.git 692973e3d937129bcbf40652eb9f2f61becf3332
git ls-remote https://github.com/actions/setup-node.git 1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a
git ls-remote https://github.com/actions/upload-pages-artifact.git 56afc609e74202658d3ffba0e8f6dda462b719fa
git ls-remote https://github.com/actions/deploy-pages.git d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e
```

Expected: each command prints the corresponding SHA. If any SHA no longer resolves, replace only that SHA with the current commit SHA for the intended action release and document the change in the commit body.

- [ ] **Step 4: Create `.github/dependabot.yml`**

Create `.github/dependabot.yml` with:

```yaml
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
```

- [ ] **Step 5: Verify every GitHub Action uses a 40-character SHA**

Run:

```powershell
$actionLines = Select-String -Path ".github\\workflows\\deploy.yml" -Pattern "uses:"
$invalid = $actionLines | Where-Object { $_.Line -notmatch "@[a-f0-9]{40}" }
if ($invalid) { $invalid; exit 1 }
```

Expected: no output.

- [ ] **Step 6: Commit GitHub automation**

Run:

```powershell
git add .github/workflows/deploy.yml .github/dependabot.yml
git commit -m "ci: add secure GitHub Pages deployment"
```

Expected: commit succeeds.

---

### Task 8: Add README And Verify Local Production Readiness

**Files:**
- Create: `README.md`

- [ ] **Step 1: Create `README.md`**

Create `README.md` with:

```markdown
# Bet Buddy

Bet Buddy ist ein lokales Partyspiel für 4, 6 oder 8 Personen. Teams treten in Bietrunden gegeneinander an und wetten darauf, wie gut ihre Buddies Challenges meistern.

## Sicherheitsmodell

Diese App ist als statische GitHub-Pages-PWA konzipiert:

- kein Backend
- keine Accounts
- keine Passwörter
- keine API-Keys
- keine Analytics
- keine externen Skripte oder Fonts
- keine externen Netzwerkrequests zur Laufzeit

Die verbindliche Sicherheitsreferenz ist `SECURITY-SSOT.md`.

## Entwicklung

```bash
npm install
npm run dev
```

## Checks

```bash
npm run typecheck
npm test
npm audit --audit-level=high
npm run security:check
npm run build
```

## Deployment

Production läuft über GitHub Pages und GitHub Actions. In den GitHub Repository Settings muss Pages auf `GitHub Actions` gesetzt werden.
```

- [ ] **Step 2: Run full local verification**

Run:

```powershell
npm run typecheck
npm test
npm audit --audit-level=high
npm run security:check
npm run build
git status --short
```

Expected:

```text
typecheck succeeds
tests pass
npm audit has no high or critical findings
security check succeeds
build succeeds
git status shows only README.md as untracked or modified
```

- [ ] **Step 3: Commit README**

Run:

```powershell
git add README.md
git commit -m "docs: add project readme"
```

Expected: commit succeeds.

- [ ] **Step 4: Verify no remote exists before publication**

Run:

```powershell
git remote -v
```

Expected: no output.

- [ ] **Step 5: Add the GitHub remote after all local checks pass**

Run:

```powershell
git remote add origin https://github.com/BetBuddies1/bet-buddy.git
```

Expected: command exits with code `0`.

- [ ] **Step 6: Verify the remote**

Run:

```powershell
git remote -v
```

Expected:

```text
origin  https://github.com/BetBuddies1/bet-buddy.git (fetch)
origin  https://github.com/BetBuddies1/bet-buddy.git (push)
```

- [ ] **Step 7: Push to GitHub**

Run:

```powershell
git push -u origin main
```

Expected: push succeeds and the repository at `https://github.com/BetBuddies1/bet-buddy` contains the committed files.

---

## Self-Review

- Spec coverage: The plan covers local repository setup first, delayed GitHub remote configuration after local verification, GitHub Pages base path, security source of truth, CSP, input validation, dependency guardrails, CI gates, Dependabot, `.npmrc`, `.nojekyll`, and production build hardening.
- Scope: This plan intentionally stops before implementing the full game flow. The next plan should cover the MVP game model and screens.
- Open-token scan: The workflow section uses concrete SHA pins and includes a verification step that fails if any action is not pinned to a 40-character SHA.
- Type consistency: `validatePlayerName` result types and tests use consistent names and return shapes.
